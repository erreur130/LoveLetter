name: Build Qt (QMake) Multi-Platform

on:
  push:
    branches: [ "main", "master" ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: linux
            qt-version: '6.8.0'
          - os: windows-latest
            target: windows
            qt-version: '6.8.0'
          - os: macos-13
            target: macos
            qt-version: '6.7.3'

    runs-on: ${{ matrix.os }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        version: ${{ matrix.qt-version }}
        target: desktop
        setup-python: true
        cache: true

    - name: Setup MSVC
      if: matrix.os == 'windows-latest'
      uses: ilammy/msvc-dev-cmd@v1

    - name: Configure (QMake)
      shell: bash
      run: |
        mkdir build
        cd build
        qmake ../FichiersQT/FichiersQT.pro

    - name: Build (Linux/macOS)
      if: matrix.os != 'windows-latest'
      run: |
        cd build
        make -j$(nproc 2>/dev/null || sysctl -n hw.ncpu)

    - name: Build (Windows)
      if: matrix.os == 'windows-latest'
      shell: cmd
      run: |
        cd build
        nmake

    # --- DÉPLOIEMENT WINDOWS ---
    - name: Deploy Windows
      if: matrix.os == 'windows-latest'
      shell: cmd
      run: |
        cd build\release
        windeployqt LoveLetter.exe --release --compiler-runtime

    # --- DÉPLOIEMENT MACOS ---
    - name: Deploy macOS
      if: matrix.os == 'macos-13'
      run: |
        cd build
        macdeployqt LoveLetter.app -dmg -always-overwrite

    # --- DÉPLOIEMENT LINUX ---
    - name: Install dependencies Linux
      if: matrix.os == 'ubuntu-22.04'
      run: |
        sudo apt-get update
        sudo apt-get install -y libfuse2 patchelf

    - name: Deploy Linux (AppImage)
      if: matrix.os == 'ubuntu-22.04'
      run: |
        cd build
        wget -O linuxdeployqt https://github.com/probonopd/linuxdeployqt/releases/download/continuous/linuxdeployqt-continuous-x86_64.AppImage
        chmod +x linuxdeployqt
        APPIMAGE_EXTRACT_AND_RUN=1 ./linuxdeployqt LoveLetter -appimage -bundle-non-qt-libs

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.target }}-build
        retention-days: 5
        path: |
          build/release/
          build/*.dmg
          build/*.AppImage